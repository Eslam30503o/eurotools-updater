# 🚀 دليل التثبيت

هذا الدليل سيساعدك في تثبيت Euro Tools Code Manager على نظامك.

## 📋 المتطلبات الأساسية

### متطلبات النظام
- **نظام التشغيل**: Windows 10/11, macOS 10.15+, Ubuntu 20.04+
- **Python**: 3.9 أو أحدث
- **الذاكرة**: 4 GB RAM (8 GB مُوصى به)
- **مساحة القرص**: 500 MB مساحة فارغة
- **الاتصال**: اتصال إنترنت للمزامنة السحابية

### التحقق من Python
```bash
# التحقق من إصدار Python
python --version
# أو
python3 --version

# يجب أن يكون الإصدار 3.9.0 أو أحدث
```

## 📦 طرق التثبيت

### الطريقة 1: التثبيت من المصدر (مُوصى به للمطورين)

#### 1. استنساخ المشروع
```bash
git clone https://github.com/Eslam30503o/eurotools-updater.git
cd euro-tools-code-manager
```

#### 2. إنشاء بيئة افتراضية
```bash
# إنشاء البيئة الافتراضية
python -m venv eurotools-env

# تفعيل البيئة الافتراضية
# على Windows:
eurotools-env\Scripts\activate

# على macOS/Linux:
source eurotools-env/bin/activate
```

#### 3. تثبيت المتطلبات
```bash
# تحديث pip
python -m pip install --upgrade pip

# تثبيت المتطلبات
pip install -r requirements.txt
```

#### 4. تشغيل التطبيق
```bash
python app.py
```

### الطريقة 2: التثبيت باستخدام pip (قريباً)

```bash
# التثبيت من PyPI (قريباً)
pip install euro-tools-code-manager

# تشغيل التطبيق
euro-tools
```

### الطريقة 3: تحميل الملف التنفيذي

1. اذهب إلى [صفحة الإصدارات](https://github.com/Eslam30503o/eurotools-updater/releases)
2. حمل أحدث إصدار لنظام التشغيل الخاص بك
3. فك الضغط وشغل الملف التنفيذي

## 🔧 الإعداد الأولي

### 1. إعداد Google Sheets (للمزامنة السحابية)

#### إنشاء مشروع Google Cloud
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد أو اختر مشروع موجود
3. فعل Google Sheets API

#### إنشاء Service Account
1. اذهب إلى "IAM & Admin" > "Service Accounts"
2. اضغط "Create Service Account"
3. املأ التفاصيل واضغط "Create"
4. أضف دور "Editor" للحساب
5. حمل ملف JSON للمفاتيح

#### إعداد الجدول
1. أنشئ جدول Google Sheets جديد
2. سمه "My Tools Sync"
3. شارك الجدول مع Service Account email
4. أعط صلاحيات "Editor"

### 2. إعداد ملف الاعتماد

ضع ملف JSON في مجلد البيانات:
```
%LOCALAPPDATA%\EuroTools\data\my-tools-sync.json  # Windows
~/.local/share/EuroTools/data/my-tools-sync.json   # Linux/macOS
```

### 3. التشغيل الأول

1. شغل التطبيق
2. أنشئ حساب مستخدم جديد
3. اختبر المزامنة مع Google Sheets
4. أضف أول أداة للاختبار

## 🧪 التحقق من التثبيت

### تشغيل الاختبارات
```bash
# اختبار سريع
python tests/test_comprehensive.py --quick

# اختبار شامل
python tests/run_tests.py

# اختبار صحة النظام
python tests/test_comprehensive.py --health
```

### التحقق من الوظائف الأساسية
1. **إنشاء أداة جديدة**: تأكد من إمكانية إضافة أداة
2. **حفظ البيانات**: تأكد من حفظ البيانات محلياً
3. **المزامنة**: اختبر المزامنة مع Google Sheets
4. **التصدير**: جرب تصدير قائمة إلى Excel

## 🔧 استكشاف مشاكل التثبيت

### مشاكل Python الشائعة

#### خطأ "Python not found"
```bash
# تأكد من تثبيت Python وإضافته لـ PATH
# على Windows، أعد تثبيت Python مع تفعيل "Add to PATH"
```

#### خطأ "pip not found"
```bash
# تثبيت pip يدوياً
python -m ensurepip --upgrade
```

### مشاكل المكتبات

#### خطأ تثبيت customtkinter
```bash
# تثبيت مع إجبار التحديث
pip install --upgrade --force-reinstall customtkinter
```

#### خطأ تثبيت pandas
```bash
# تثبيت مع dependencies إضافية
pip install pandas[all]
```

### مشاكل Google Sheets

#### خطأ "Credentials not found"
- تأكد من وضع ملف JSON في المكان الصحيح
- تحقق من صلاحيات الملف

#### خطأ "Permission denied"
- تأكد من مشاركة الجدول مع Service Account
- تحقق من صلاحيات "Editor"

## 🔄 التحديث

### تحديث من المصدر
```bash
# سحب أحدث التغييرات
git pull origin main

# تحديث المتطلبات
pip install -r requirements.txt --upgrade
```

### التحديث التلقائي
التطبيق يدعم التحديث التلقائي:
1. سيفحص التحديثات كل 12 ساعة
2. سيعرض إشعار عند توفر تحديث
3. يمكن التحديث بنقرة واحدة

## 🗑️ إلغاء التثبيت

### إزالة البيئة الافتراضية
```bash
# إلغاء تفعيل البيئة
deactivate

# حذف مجلد البيئة
rm -rf eurotools-env  # Linux/macOS
rmdir /s eurotools-env  # Windows
```

### إزالة البيانات
```bash
# حذف مجلد البيانات (اختياري)
# Windows: %LOCALAPPDATA%\EuroTools
# Linux/macOS: ~/.local/share/EuroTools
```

## 📞 الحصول على المساعدة

إذا واجهت مشاكل في التثبيت:

1. **راجع الأسئلة الشائعة**: [FAQ](help/faq.md)
2. **استكشاف الأخطاء**: [Troubleshooting](help/troubleshooting.md)
3. **أنشئ Issue**: [GitHub Issues](https://github.com/your-username/euro-tools-code-manager/issues)
4. **راسلنا**: support@eurotools.com

## 📋 قائمة التحقق

- [ ] Python 3.9+ مثبت
- [ ] البيئة الافتراضية منشأة ومفعلة
- [ ] المتطلبات مثبتة بنجاح
- [ ] Google Sheets API معد (للمزامنة)
- [ ] التطبيق يعمل بدون أخطاء
- [ ] الاختبارات تنجح
- [ ] البيانات تحفظ وتحمل بنجاح

---


**التالي**: [الإعداد الأولي](setup.md)
