# ⚙️ الإعداد الأولي

دليل شامل للإعداد الأولي لـ Euro Tools Code Manager بعد التثبيت.

## 🎯 نظرة عامة

بعد تثبيت التطبيق بنجاح، تحتاج إلى إجراء بعض الإعدادات الأولية لضمان عمل جميع الميزات بشكل صحيح.

## 🚀 الخطوات الأساسية

### 1. تشغيل التطبيق لأول مرة

```bash
# تشغيل التطبيق
python app.py
```

عند التشغيل الأول، سيقوم التطبيق بـ:
- إنشاء ملفات البيانات الأساسية
- إعداد المجلدات المطلوبة
- تحميل الإعدادات الافتراضية

### 2. إعداد حساب المستخدم الأول

#### إنشاء حساب إداري
1. انقر على "إنشاء حساب جديد"
2. أدخل البيانات التالية:
   - **اسم المستخدم**: admin (مُوصى به)
   - **كلمة المرور**: كلمة مرور قوية
   - **البريد الإلكتروني**: بريدك الإلكتروني
   - **النوع**: مدير

#### معايير كلمة المرور القوية
- 8 أحرف على الأقل
- تحتوي على أحرف كبيرة وصغيرة
- تحتوي على أرقام
- تحتوي على رموز خاصة

### 3. إعداد المزامنة السحابية (اختياري)

#### تفعيل Google Sheets
1. اذهب إلى **الإعدادات** > **المزامنة**
2. انقر على "إعداد Google Sheets"
3. اتبع التعليمات لربط حسابك
4. اختر جدول البيانات أو أنشئ جديد

#### إعداد المزامنة التلقائية
```
⚙️ إعدادات المزامنة المُوصى بها:
├── المزامنة التلقائية: مفعلة
├── فترة المزامنة: كل 5 دقائق
├── مزامنة عند البدء: مفعلة
└── مزامنة عند الإغلاق: مفعلة
```

## 🎨 تخصيص الواجهة

### اختيار المظهر
1. **الإعدادات** > **المظهر**
2. اختر من:
   - المظهر الفاتح
   - المظهر المظلم
   - تلقائي (حسب النظام)

### تخصيص الألوان
- **اللون الأساسي**: أزرق (افتراضي)
- **لون التمييز**: أزرق فاتح
- **خط الواجهة**: Segoe UI (Windows) / SF Pro (Mac)

### إعداد اللغة
- **العربية**: اللغة الافتراضية
- **الإنجليزية**: متاحة كخيار ثاني

## 📊 إعداد فئات الأدوات

### الفئات الافتراضية
سيتم إنشاء الفئات التالية تلقائياً:

```
🔧 أدوات القطع
├── مثاقب
├── مناشير
└── أدوات التشكيل

🔩 أدوات الربط
├── مفاتيح ربط
├── مفكات
└── كماشات

📏 أدوات القياس
├── مساطر
├── زوايا
└── أجهزة قياس

⚡ أدوات كهربائية
├── مثاقب كهربائية
├── منشار كهربائي
└── صاروخ كهربائي
```

### إضافة فئات مخصصة
1. اذهب إلى **إدارة الفئات**
2. انقر على "إضافة فئة جديدة"
3. أدخل:
   - اسم الفئة
   - الوصف
   - الفئة الأب (اختياري)

## 🛠️ إعداد قوالب الأكواد

### القوالب الافتراضية
```
📋 قوالب الأكواد المُعدة مسبقاً:
├── أدوات القطع: CT-XXXX
├── أدوات الربط: FT-XXXX  
├── أدوات القياس: MT-XXXX
└── أدوات كهربائية: ET-XXXX
```

### إنشاء قوالب مخصصة
1. **الإعدادات** > **قوالب الأكواد**
2. انقر على "إضافة قالب جديد"
3. حدد:
   - نمط الكود (مثل: ABC-####)
   - الفئة المرتبطة
   - البادئة والنهاية

## 📁 تنظيم البيانات

### هيكل المجلدات
```
Euro Tools Data/
├── 📁 tools.json          # بيانات الأدوات
├── 📁 lists.json          # قوائم المشاريع
├── 📁 users.json          # بيانات المستخدمين
├── 📁 settings.json       # إعدادات التطبيق
├── 📁 backups/            # النسخ الاحتياطية
│   ├── daily/             # نسخ يومية
│   ├── weekly/            # نسخ أسبوعية
│   └── monthly/           # نسخ شهرية
└── 📁 exports/            # الملفات المُصدرة
    ├── excel/             # ملفات Excel
    ├── pdf/               # ملفات PDF
    └── images/            # الصور والباركود
```

### إعداد النسخ الاحتياطية التلقائية
1. **الإعدادات** > **النسخ الاحتياطية**
2. فعل النسخ التلقائية:
   - نسخة يومية: عند إغلاق التطبيق
   - نسخة أسبوعية: كل أحد
   - نسخة شهرية: أول كل شهر

## 🔐 إعدادات الأمان

### تفعيل ميزات الأمان
```
🔒 إعدادات الأمان المُوصى بها:
├── تشفير البيانات الحساسة: مفعل
├── انتهاء صلاحية الجلسة: 8 ساعات
├── سجل النشاط: مفعل
├── تأكيد العمليات الحساسة: مفعل
└── نسخ احتياطية مشفرة: مفعل
```

### إعداد صلاحيات المستخدمين
```
👤 أنواع المستخدمين:
├── مدير: جميع الصلاحيات
├── محرر: إضافة وتعديل البيانات
├── مشاهد: عرض البيانات فقط
└── ضيف: وصول محدود
```

## 📊 إعداد التقارير

### تخصيص التقارير الافتراضية
1. **التقارير** > **إعدادات التقارير**
2. اختر التقارير المطلوبة:
   - تقرير المخزون الشامل
   - تقرير الأدوات حسب الفئة
   - تقرير استخدام الأدوات
   - تقرير المشاريع النشطة

### إعداد التصدير التلقائي
- **تنسيق التصدير**: Excel + PDF
- **جدولة التقارير**: أسبوعياً
- **مجلد الحفظ**: تلقائي
- **إرسال بالبريد**: اختياري

## 🌐 إعدادات الشبكة

### إعداد البروكسي (إذا لزم الأمر)
```
🌐 إعدادات البروكسي:
├── نوع البروكسي: HTTP/HTTPS
├── عنوان الخادم: proxy.company.com
├── المنفذ: 8080
├── المصادقة: حسب الحاجة
└── استثناءات: localhost, 127.0.0.1
```

### إعدادات جدار الحماية
تأكد من السماح للتطبيق بالوصول إلى:
- Google Sheets API (sheets.googleapis.com)
- خدمات التحديث (github.com)
- خدمات النسخ الاحتياطي السحابية

## ✅ التحقق من الإعداد

### قائمة التحقق النهائية
```
☑️ قائمة التحقق من الإعداد:
├── ☑️ تم إنشاء حساب المدير
├── ☑️ تم إعداد المزامنة السحابية
├── ☑️ تم تخصيص الواجهة
├── ☑️ تم إعداد الفئات والقوالب
├── ☑️ تم تفعيل النسخ الاحتياطية
├── ☑️ تم إعداد الأمان
├── ☑️ تم اختبار التقارير
└── ☑️ تم التحقق من الاتصال
```

### اختبار الوظائف الأساسية
1. **إضافة أداة جديدة**: تأكد من عمل النظام
2. **إنشاء قائمة مشروع**: اختبر إدارة القوائم
3. **تصدير تقرير**: تأكد من عمل التصدير
4. **المزامنة**: اختبر المزامنة السحابية

## 🆘 استكشاف مشاكل الإعداد

### المشاكل الشائعة وحلولها

#### مشكلة: فشل في إنشاء ملفات البيانات
```
الحل:
1. تأكد من صلاحيات الكتابة في المجلد
2. شغل التطبيق كمدير (Windows)
3. تحقق من مساحة القرص المتاحة
```

#### مشكلة: فشل المزامنة مع Google Sheets
```
الحل:
1. تحقق من اتصال الإنترنت
2. تأكد من صحة بيانات الاعتماد
3. تحقق من إعدادات جدار الحماية
4. أعد تفويض التطبيق
```

#### مشكلة: بطء في الأداء
```
الحل:
1. قلل فترة المزامنة التلقائية
2. أغلق التطبيقات غير المستخدمة
3. تحقق من مساحة القرص
4. أعد تشغيل التطبيق
```

## 📞 الحصول على المساعدة

إذا واجهت مشاكل في الإعداد:

1. **راجع الأسئلة الشائعة**: [FAQ](help/faq.md)
2. **دليل استكشاف الأخطاء**: [Troubleshooting](help/troubleshooting.md)
3. **تواصل مع الدعم**: support@eurotools.com
4. **منتدى المجتمع**: [GitHub Discussions](https://github.com/your-username/euro-tools-code-manager/discussions)

---

**التالي**: [أول استخدام](getting-started.md)